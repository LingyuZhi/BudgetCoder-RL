"""Data helpers.

M1A: official SWE-Gym raw acquisition and profile.
M1B: field visibility contract and non-filtering integrity / leakage audit.
M1C-A: gold-patch file / hunk / line-coordinate oracles
       (`gold_edit_files` vs `base_changed_files`; no AST).
M1C-B: base-repository function/class symbol oracles (stdlib AST).
M1D-A: eligibility / structural-difficulty features (no filter / split).
M1D-B: eligibility policy freeze and grouped/repo-stratified train/dev split.
"""
