# M3C / M4 default AgentLoop registry. Same loop class as M3B.
# Frozen by configs/experiments/stage1_m3c_freeze.json:
#   primary_training_B_obs=4096, budget_visible=true, eval set {2048,4096,8192}.
# Runners may still override extra_info.obs_tokens_limit for eval budgets.
- name: repo_exploration
  _target_: budget_coder_rl.agent_loop.repo_exploration.RepoExplorationAgentLoop
  max_turns: 6
  max_new_tokens_per_turn: 2048
  obs_tokens_limit: 4096
  budget_visible: true
